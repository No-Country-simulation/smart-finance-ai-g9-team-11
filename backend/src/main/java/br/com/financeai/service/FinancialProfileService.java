package br.com.financeai.service;

import org.springframework.stereotype.Service;

/**
 * Placeholder: ainda não tem implementação.
 *
 * Reservada para uma futura lógica de perfil financeiro que hoje é
 * calculada inteiramente pela IA externa (via MlClient/MlResponse).
 * Se algum dia parte desse cálculo passar a ser feita localmente no
 * backend (ex: regras determinísticas em cima das transações, sem
 * depender da IA), é aqui que essa lógica entraria.
 */
@Service
public class FinancialProfileService {
}